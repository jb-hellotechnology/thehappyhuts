# Local secrets — never commit
.env
.env.example

# Perch config (DB credentials, license key) — environment-specific, keep on server only
/perch/config/config*

# Composer dependencies (restore with `composer install`)
/vendor/

# Perch / PHP runtime error logs (some are multiple GB)
error_log

# macOS
.DS_Store
